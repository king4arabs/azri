# Claude Code instructions for the `web/` workspace

This file exists so that Claude Code (and any other agent that follows the
`CLAUDE.md` convention) picks up the same rules as every other agent working
in this workspace. The single source of truth lives in `AGENTS.md`; the
`@AGENTS.md` import below pulls those rules in verbatim so they do not drift
out of sync.

If you are reading this manually, open [`AGENTS.md`](./AGENTS.md) directly.

@AGENTS.md
