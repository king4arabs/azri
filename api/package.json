{
  "name": "@azri/api",
  "version": "0.1.0",
  "private": true,
  "description": "AZRI ingestion + core API. Fastify, OpenAPI, structured logging, Zod-validated request bodies, idempotent watch and Whoop webhook endpoints.",
  "license": "UNLICENSED",
  "type": "module",
  "main": "src/server.ts",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test src/__tests__/*.test.ts",
    "build": "tsc"
  },
  "dependencies": {
    "@azri/contracts": "*",
    "@fastify/helmet": "^12.0.1",
    "@fastify/rate-limit": "^10.2.1",
    "@fastify/sensible": "^6.0.1",
    "@fastify/swagger": "^9.4.0",
    "@fastify/swagger-ui": "^5.2.1",
    "fastify": "^5.1.0",
    "fastify-plugin": "^5.0.1",
    "pino": "^9.5.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4.19.2",
    "typescript": "^5"
  }
}
